// Theme switching is disabled — the site is permanently dark.
// This stub exists so imports elsewhere don't break during the transition.
export function useThemeSwitch() {
  return ["dark", () => {}];
}
